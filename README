0: clone repository

1. requirements:
    1.0: homebrew (manual prepend "/opt/homebrew/bin" to $PATH for now)
    1.1: fonts
        1.1.1: OperatorMono for ascii 
        1.1.2: brew install font-hack-nerd-font (setup for non-ascii in iterm2)
    1.2: on-my-zsh

2. iterm2 & tmux
    2.1: base16-google-light.itermcolors
    2.2: brew install neovim tmux git wget npm(for jsonls and yamlls)
    2.3: enable italic text && turn off "Show mark indicators" for iterm2
    2.4: tic -x tmux/tmux-256color.terminfo  (setup tmux to enable italic text)
    2.5: silent bell(!!!)

3. lua(lua_ls for lsp, luacheck for lint, stylua for format, installed by mason)
    3.0: install luarocks first(https://luarocks.org/#quick-start)

4. python(pylsp for lsp, ruff for lint, ruff-format for format, installed by mason)
    4.0: brew install pyenv
    4.1: pyenv install 3.10 & pyenv global 3.10

5. ruby(ruby-lsp for lsp, rubocop for ruby lint & format, erb-lint/erb-formatter for eruby lint/format, installed by mason)
    5.0: brew install rbenv
    5.1: rbenv install 3.3.5 & rbenv global 3.3.5

6. mysql-client
    6.0: brew install mysql-client@8.0
    6.1: echo 'export PATH="/opt/homebrew/opt/mysql-client@8.0/bin:$PATH"' >> ~/.zshrc

7. coding
    7.1: npm i -g @openai/codex 

7. install
    7.1: chmod +x install && ./install
